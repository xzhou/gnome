function [phasedSeq] = getPhaseSeq(id, fastaSeq)
    [fm fn] = size(fastaSeq);
    [im in] = size(id);
    
    selectedIndex = [];
    
    for i = 1:im
        idName = id(i);
        for j = 1:fm
            
            
        end
    end
    
end