function [nextBlock] = getNextBlock(blocks, minBSize)
    %get Next block will return the row number of the next block for
    %learnning, we first seach the neighbour blocks with block size
    %larger than bsize
    
    if nargin == 1
        minBSize = 2;
    end
    
    
end